{
  "name": "RequestNetwork",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ Christopher-I/RequestNetwork",
    "create-local": "graph create --node http://localhost:8020/ Christopher-I/RequestNetwork",
    "remove-local": "graph remove --node http://localhost:8020/ Christopher-I/RequestNetwork",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 Christopher-I/RequestNetwork"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.13.3",
    "@graphprotocol/graph-ts": "0.13.0"
  }
}
